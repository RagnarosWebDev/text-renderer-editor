import {VDomRenderer, VNode} from "../../text-renderer/lib";

const plugins = [];

export class VEditor {
    root: Element;
    currentVNode: number;
    renderer: VDomRenderer;

    constructor(root: Element, nodes: string) {

    }
}